<% provide(:title, @funder.name) %>

<div id="initiative">
  <h1><%= @funder.name %></h1>

  <div class="stdcontainer">
    <%= link_to "<i class='glyphicon glyphicon-edit'></i> Edit".html_safe, edit_funder_path, class: 'btn btn-default edit-btn' %>
    <%= link_to "<i class='glyphicon glyphicon-trash'></i> Delete".html_safe, @funder, method: :delete,
                data: {confirm: "Are you sure you want to delete this funder?"},
                class: "btn btn-default delete-btn" %>
    <h3><%= @funder.description %></h3>
    <h3><%= @funder.address %></h3>
    <h3><%= @funder.telephone %></h3>
    <h3><%= @funder.email %></h3>
    <h3><%= @funder.url %></h3>
    <br>
    <h3>Initiatives Affected:</h3>
    <% @funder.initiative_funders.each do |funding| %>
       <h3><%= funding.initiative.name %></h3>
    <% end %>
    <h3>Users Affected:</h3>
    <% @funder.initiative_funders.each do |funding| %>
      <% funding.initiative.users.each do |user| %>
        <h3><%= user.name %></h3>
      <% end %>
    <% end %>
  </div>
</div>
